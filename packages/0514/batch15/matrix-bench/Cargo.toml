[package]
name = "matrix-bench"
version = "0.1.0"
edition = "2021"

[[bench]]
name = "matrix_bench"
harness = false

[dependencies]
matrix-core = { path = "../matrix-core" }
matrix-sparse = { path = "../matrix-sparse" }
rand = "0.8"

[dev-dependencies]
criterion = "0.5"
