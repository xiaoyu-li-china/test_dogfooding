[package]
name = "matrix-core"
version = "0.1.0"
edition = "2021"

[features]
default = ["std"]
std = []

[dependencies]
num-traits = { version = "0.2", default-features = false, features = ["libm"] }

[dev-dependencies]
proptest = "1.4"
